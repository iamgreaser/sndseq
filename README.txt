this is an attempt to emulate the /dev/snd/seq thing of ALSA as a FreeBSD KLD (kernel module).

the requirements are quite low.

hoping to slap a BSD licence on this - in the meantime i've credited where i've taken the base code from.

